{
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.5.3"
  },
  "scripts": {
    "prepare": "husky",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install"
  }
}
